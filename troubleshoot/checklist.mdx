---
title: Final Checklist
description: Verify your MetaHash miner is fully configured and operational
---

# Final Checklist

Use this checklist to ensure your MetaHash miner is properly configured and running.

## Environment & Software

<Steps>
  <Step title="Python 3.12 Installed">
    ```bash
    python3 --version
    ```
    <Check>Should show Python 3.8 or higher</Check>
  </Step>

  <Step title="Virtual Environment Active">
    ```bash
    which python
    ```
    <Check>Should show path to `.venv/bin/python`</Check>
  </Step>

  <Step title="Dependencies Installed">
    ```bash
    pip list | grep -E "bittensor|async-substrate"
    ```
    <Check>Both packages should be listed</Check>
  </Step>

  <Step title="Bittensor CLI Working">
    ```bash
    btcli --version
    ```
    <Check>Should display version number</Check>
  </Step>
</Steps>

---

## Wallet & Registration

<Steps>
  <Step title="Wallet Created">
    ```bash
    btcli wallet list
    ```
    <Check>Your wallet should be listed</Check>
  </Step>

  <Step title="Hotkey Created">
    ```bash
    btcli wallet list --wallet.name mywallet
    ```
    <Check>Your hotkey should be visible</Check>
  </Step>

  <Step title="Registered on Subnet 73">
    ```bash
    btcli subnets metagraph --netuid 73 | grep YOUR_HOTKEY
    ```
    <Check>Should show your UID and hotkey</Check>
  </Step>

  <Step title="Wallet Backed Up">
    <Check>Seed phrases saved securely offline</Check>
    <Check>Wallet folder backed up: `~/.bittensor/wallets/`</Check>
  </Step>
</Steps>

---

## Configuration

<Steps>
  <Step title=".env File Exists">
    ```bash
    ls -la ~/metahash/.env
    ```
    <Check>File should exist</Check>
  </Step>

  <Step title=".env Contains Required Variables">
    ```bash
    cat ~/metahash/.env
    ```
    <Check>
    Should include:
    - `WALLET_NAME`
    - `WALLET_HOTKEY`
    - `WALLET_PASSWORD`
    - `BITTENSOR_NETWORK=finney`
    </Check>
  </Step>

  <Step title=".env in .gitignore">
    ```bash
    grep ".env" ~/metahash/.gitignore
    ```
    <Check>.env should be listed to prevent accidental commits</Check>
  </Step>
</Steps>

---

## Staking & Funding

<Steps>
  <Step title="TAO Staked on Funding Subnet">
    ```bash
    btcli stake overview --wallet.name mywallet --wallet.hotkey miner1
    ```
    <Check>Should show stake on subnet 62 (or your funding subnet)</Check>
    <Check>Minimum: 0.10 TAO recommended</Check>
  </Step>

  <Step title="Payment Validator Has Stake">
    ```bash
    btcli stake overview
    ```
    <Check>The hotkey used in `--payment.validators` must have stake</Check>
  </Step>

  <Step title="α Balance Available">
    <Check>Stake overview should show α tokens for your funding subnet</Check>
  </Step>
</Steps>

---

## Network & Connectivity

<Steps>
  <Step title="Port 8091 Open in AWS">
    <Check>AWS Security Group has inbound rule for TCP 8091</Check>
    <Check>Source: 0.0.0.0/0 (or specific validator IPs)</Check>
  </Step>

  <Step title="Port Listening Locally">
    ```bash
    ss -tulpn | grep 8091
    ```
    <Check>Should show Python listening on [::]:8091</Check>
  </Step>

  <Step title="Port Accessible Externally">
    ```bash
    nc -zv YOUR_INSTANCE_IP 8091
    ```
    <Check>Should show "Connection succeeded"</Check>
  </Step>

  <Step title="UFW Configured (if enabled)">
    ```bash
    sudo ufw status
    ```
    <Check>If active, port 8091/tcp should be allowed</Check>
  </Step>
</Steps>

---

## Miner Operation

<Steps>
  <Step title="Miner Starts Successfully">
    ```bash
    python neurons/miner.py --netuid 73 ...
    ```
    <Check>Should see "Miner started" in logs</Check>
  </Step>

  <Step title="Axon Served">
    <Check>Logs show: `Axon served with: AxonInfo(...:8091)`</Check>
  </Step>

  <Step title="Bid Lines Configured">
    <Check>
    Logs show:
    ```
    Configured Bid Lines:
      0 │ 62 │ 0.2500 α │ 500 bps
    ```
    </Check>
  </Step>

  <Step title="Miner Running">
    <Check>Logs periodically show "Miner running…"</Check>
  </Step>

  <Step title="No Password Prompts">
    <Check>Miner runs without asking for wallet password</Check>
  </Step>
</Steps>

---

## Auction Activity

<Steps>
  <Step title="AuctionStart Events">
    ```bash
    grep "AuctionStart" ~/.bittensor/miners/mywallet/miner1/netuid73/miner.log
    ```
    <Check>Should see auction events (may take time to appear)</Check>
  </Step>

  <Step title="Win Invoices Received">
    ```bash
    grep "Win invoice" ~/.bittensor/miners/mywallet/miner1/netuid73/miner.log
    ```
    <Check>Indicates successful bid wins (may take time)</Check>
  </Step>

  <Step title="α Payments Sent">
    ```bash
    grep "α payment sent" ~/.bittensor/miners/mywallet/miner1/netuid73/miner.log
    ```
    <Check>Confirms payments are being made to validators</Check>
  </Step>
</Steps>

<Info>
Auction events may not appear immediately. Give it some time for validators to initiate auctions and for your miner to participate.
</Info>

---

## Long-Running Setup

<Tabs>
  <Tab title="Screen">
    <Steps>
      <Step title="Screen Session Created">
        ```bash
        screen -ls
        ```
        <Check>Your miner session should be listed</Check>
      </Step>

      <Step title="Can Detach/Reattach">
        <Check>`Ctrl+A D` detaches</Check>
        <Check>`screen -r miner` reattaches</Check>
      </Step>
    </Steps>
  </Tab>

  <Tab title="Systemd">
    <Steps>
      <Step title="Service File Created">
        ```bash
        ls -la /etc/systemd/system/metahash-miner.service
        ```
        <Check>Service file should exist</Check>
      </Step>

      <Step title="Service Enabled">
        ```bash
        sudo systemctl is-enabled metahash-miner
        ```
        <Check>Should return "enabled"</Check>
      </Step>

      <Step title="Service Running">
        ```bash
        sudo systemctl status metahash-miner
        ```
        <Check>Should show "active (running)"</Check>
      </Step>

      <Step title="Auto-Restart Configured">
        <Check>Service file has `Restart=always`</Check>
      </Step>

      <Step title="Logs Accessible">
        ```bash
        journalctl -u metahash-miner -n 10
        ```
        <Check>Should show recent miner logs</Check>
      </Step>
    </Steps>
  </Tab>
</Tabs>

---

## System Resources

<Steps>
  <Step title="Sufficient Memory">
    ```bash
    free -h
    ```
    <Check>At least 1GB available RAM</Check>
    <Check>Swap space configured (optional but recommended)</Check>
  </Step>

  <Step title="Sufficient Disk Space">
    ```bash
    df -h
    ```
    <Check>At least 5GB free space</Check>
  </Step>

  <Step title="Clock Synchronized">
    ```bash
    timedatectl status
    ```
    <Check>"System clock synchronized: yes"</Check>
  </Step>

  <Step title="No Memory Leaks">
    <Check>Monitor over time: `htop` or `free -h`</Check>
    <Check>Memory usage should be stable</Check>
  </Step>
</Steps>

---

## Monitoring & Maintenance

<Steps>
  <Step title="Log Monitoring Setup">
    ```bash
    tail -f ~/.bittensor/miners/mywallet/miner1/netuid73/miner.log
    ```
    <Check>Can view real-time logs</Check>
  </Step>

  <Step title="Health Check Script">
    <Check>Optional: Create monitoring script (see [Commands](/troubleshoot/commands))</Check>
  </Step>

  <Step title="Backup Strategy">
    <Check>Wallet backed up</Check>
    <Check>`.env` file backed up securely</Check>
    <Check>Configuration documented</Check>
  </Step>

  <Step title="Update Strategy">
    <Check>Know how to update MetaHash: `git pull`</Check>
    <Check>Know how to update dependencies</Check>
  </Step>
</Steps>

---

## Final Verification Matrix

| Category | Item | Status |
|----------|------|--------|
| **Environment** | Python 3.12 | ✅ |
| | Virtual environment activated | ✅ |
| | Dependencies installed | ✅ |
| **Wallet** | Coldkey created | ✅ |
| | Hotkey created | ✅ |
| | Registered on SN73 | ✅ |
| | Seed phrases backed up | ✅ |
| **Configuration** | .env exists | ✅ |
| | Password configured | ✅ |
| **Staking** | TAO staked (≥0.10) | ✅ |
| | α balance available | ✅ |
| **Network** | Port 8091 open | ✅ |
| | Port listening | ✅ |
| | Externally accessible | ✅ |
| **Miner** | Starts successfully | ✅ |
| | Axon served | ✅ |
| | Bid lines configured | ✅ |
| | Logs show activity | ✅ |
| **Long-Running** | Screen or systemd configured | ✅ |
| | Auto-restart enabled | ✅ |

---

## Common Post-Setup Issues

<AccordionGroup>
  <Accordion title="Miner runs but no auctions">
    **Wait Time**: Auctions may not start immediately

    **Check**:
    - Port 8091 externally accessible
    - Validators are active on subnet 73
    - Payment validator has stake

    **Timeline**: Can take hours or days for first auction
  </Accordion>

  <Accordion title="High memory usage">
    **Normal**: Python processes can use 500MB-1GB

    **Monitor**: Use `htop` to track over time

    **Fix if growing**: Add swap or restart periodically
  </Accordion>

  <Accordion title="Occasional restarts">
    **Normal**: Some restarts are expected

    **Check**: systemd should auto-restart

    **Investigate if frequent**: Check logs for errors
  </Accordion>
</AccordionGroup>

---

## You're All Set!

<Check>
If all items above are checked, your MetaHash miner is fully operational! 🎉
</Check>

## What's Next?

- **Monitor logs** regularly for auction activity
- **Join the community** on [Discord](https://discord.com/channels/799672011265015819/1351969903132938302)
- **Follow updates** on [Twitter](https://x.com/metahashsn73)
- **Review** the [Troubleshooting](/troubleshoot/troubleshooting) guide if issues arise
- **Read** the [UID111 Review](/review/uid111-review) for real-world setup experience

---

## Need Help?

If anything isn't working:

1. **Check logs**: `tail -f ~/.bittensor/miners/mywallet/miner1/netuid73/miner.log`
2. **Review** [Troubleshooting](/troubleshoot/troubleshooting)
3. **Ask for help** on [Discord](https://discord.com/channels/799672011265015819/1351969903132938302)
4. **Reference** [Helpful Commands](/troubleshoot/commands)

<Tip>
Keep this checklist handy for future reference when setting up additional miners or troubleshooting issues.
</Tip>
